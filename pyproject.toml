[project]
name = "x-search-plugin"
version = "0.1.0"
description = "MCP plugin for read-only X/Twitter research through xAI x_search."
readme = "README.md"
requires-python = ">=3.9"
license = "MIT"
dependencies = []

[tool.uv]
package = false
